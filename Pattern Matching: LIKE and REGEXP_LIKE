select empno, ename, sal from emp 
where ename like '%A%'

select empno, ename, sal from emp 
where ename like '%M'

select empno, ename, sal from emp 
where ename like '_I_G'

select ename from emp
where regexp_like(ename,'S')
order by ename;
